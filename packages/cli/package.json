{
  "name": "@sorostream/cli",
  "version": "0.1.0",
  "description": "CLI tool for the SoroStream payment streaming protocol",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "sorostream": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "lint": "tsc --noEmit"
  },
  "keywords": ["stellar", "soroban", "streaming", "payments", "cli"],
  "license": "MIT",
  "dependencies": {
    "@sorostream/sdk": "^0.1.0",
    "@stellar/stellar-sdk": "^13.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "files": ["dist"],
  "repository": {
    "type": "git",
    "url": "https://github.com/SoroStream/sorostream-sdk.git"
  }
}
